import React from 'react';

export const Navbar = () => {
    return (
        <div className="container">
            <h1>--appNavbar--</h1>

            <button type="button" className="btn btn-primary">
            Launch demo modal
            </button>
        </div>
    );
}